-- ⚠️ FILE NÀY ĐÃ LỖI THỜI — KHÔNG CHẠY
--
-- seeding.sql cũ (mysqldump) còn tham chiếu các bảng đã bỏ trong schema v4:
--   calibration_rounds, calibration_scores, categories, category_mentors, ...
--
-- Dùng thứ tự sau:
--   1. hackathon_mysql_v4.sql      (schema — chạy trước)
--   2. hackathon_mysql_v4_seed.sql (seed data — chạy sau)
